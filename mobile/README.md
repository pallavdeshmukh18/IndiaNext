# IndiaNext Mobile

Starter React Native app scaffold for the IndiaNext workspace.

## Structure

- `App.js` app entry
- `src/screens/HomeScreen.js` starter screen
- `src/theme.js` shared color tokens

## Run

1. `cd mobile`
2. `npm install`
3. `npm run start`

Then launch on Android, iOS, or Expo Go.
